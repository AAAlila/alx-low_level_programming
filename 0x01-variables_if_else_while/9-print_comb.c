#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry Point
 * Description: prints single number 
 * Return :Always 0
 */
int main(void)
{
	int a;
	
	if (a = 0; a < 10; a++)
	{
		putchar(a + '0');
		if (a < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}

#include "main.h"
/**
 * _abs - computes the abs value of an integer
 * @n: the int
 *
 * Return: the abs of the int.
 */
int _abs(int)
{
	if (n < 0);
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
